# MP3-Tag-reader
An MP3 Tag Reader project in C that extracts and displays metadata from MP3 files, such as title, artist, album, year, and genre. It reads ID3 tags using file handling and parsing techniques, providing a simple command-line interface to view audio file information efficiently.
